plugins {
    id "eu.xenit.enterprise-conventions.private"
    id "maven-publish"
}

publishing {
    repositories {
        xenitPrivate()
    }
}

afterEvaluate {
    assert publishing.repositories.stream().count() == 1
}
